%h1 All Categories
%table{:class => "table table-striped table-bordered table-condensed"}
  %thead
    %tr
      %th Name
  %tbody
    - @categories.each do |category|
      %tr
        %th= link_to category.name, edit_category_path(category)

